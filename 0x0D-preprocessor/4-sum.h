#ifndef _4_SUM_H_
#define _4_SUM_H_

/**
 * main - Entry point
 * Descr: Header file that defines a function-like macro
 * SUM(x, y), that computes
 * @x: sum of the numbers
 * @y: the sum of macro
 */

#define SUM(x, y) ((x) + (y))

#endif
